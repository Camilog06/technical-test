import "./chunk-AUZ3RYOM.js";

// node_modules/.pnpm/redux-immer@1.0.5/node_modules/redux-immer/src/redux-immer.js
var combineReducers = (produce, reducers = {}) => {
  const keys = Object.keys(reducers);
  const initialState = keys.reduce((a, k) => {
    a[k] = reducers[k](void 0, {});
    return a;
  }, {});
  return produce((draft, action) => {
    for (let i = 0; i < keys.length; i += 1) {
      const key = keys[i];
      draft[key] = reducers[key](draft[key], action);
    }
    return draft;
  }, initialState);
};
export {
  combineReducers
};
//# sourceMappingURL=redux-immer.js.map
